import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bar: {
    position: 'absolute',
    flexDirection: 'row',
    top: 55,
    left: 10,
    zIndex: 10,
  },
  icon: {
    margin: 2,
  },
});
