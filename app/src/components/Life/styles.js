import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  bar: {
    position: 'absolute',
    flexDirection: 'row',
    top: 45,
    left: 10,
    zIndex: 10,
  },
  icon: {
    marginRight: 3,
  },
});
